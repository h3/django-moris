from django.conf.urls.defaults import *
from django.conf import settings

from moris.viewports.views import *

urlpatterns=patterns('',
#   url(r'^$',     ViewportView.as_view(), name='moris-viewport'),
)

